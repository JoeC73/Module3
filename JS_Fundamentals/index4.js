let a = 8, b = 3;
let result = `${a} + ${b} is `;

console.log( a + b < 10 ? 'less than 10' : 'greater than 10 ')


// First it was less than ten because the total of the values were 5.

// Next I added an 8 as a value and the result is greater than 10.

/*The += is a shortcut that increments the mathematical operation. 
a + b is the result of += which is 5. */